/** @deprecated Import from `@/lib/seo` barrel — re-exports for backward compatibility */
export {
  buildMetadata,
  buildRootMetadata,
  buildPageMetadata,
  getPageMeta,
  type BuildMetadataOptions,
  type SeoPageKey,
} from "./seo/index";
